# ════════════════════════════════════════════════════════════════
# RegRadar -- Python Dependencies (verified May 23, 2026)
# Stack: Gemini 3.5 Flash + Pydantic AI v1.85+ + ClickHouse 25.8+ + ddtrace 4.8+
# ════════════════════════════════════════════════════════════════

# ─── Core Web ───
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.1
python-dotenv>=1.0.1
pydantic>=2.9.2
pydantic-settings>=2.5.2
httpx>=0.27.2
aiohttp>=3.10.10

# ─── Pydantic AI (the agent framework) ───
# v1.63+ required for full Datadog ddtrace instrumentation of tool calls
# v1.85+ has the GoogleVertexProvider we use in vertex_ai.py
pydantic-ai>=1.85.0

# ─── LLM / AI ───
# google-genai is the UNIFIED SDK (replaces deprecated google.generativeai)
# Pydantic AI uses google-genai under the hood for the Vertex AI provider
google-genai>=2.0.0
google-cloud-discoveryengine>=0.13.5      # Check Grounding API (the Auditor's spine)
openai>=1.54.4                            # OpenRouter fallback (OpenAI-compatible API)

# ─── Data ───
clickhouse-connect[arrow]>=0.8.0          # 0.8+ supports ClickHouse 25.8 vector_similarity index
pandas>=2.2.3
numpy>=2.1.2
pyarrow>=18.0.0

# ─── Web Scraping (Nimble Web Search Agents Platform) ───
# Package is `nimble_python` on PyPI (the official Nimble web SDK).
# `nimble-sdk` (no underscore) is HPE Nimble Storage -- different product.
nimble_python>=0.18.0
firecrawl-py>=1.6.7                       # silent fallback

# ─── Senso (publish to cited.md) ───
# No official Python SDK -- we call the REST API directly via httpx
# Docs: https://docs.senso.ai

# ─── x402 / Coinbase Developer Platform (monetization) ───
# Optional but high-value for the "Monetize" beat in the demo
x402>=0.4.0                               # x402.fastapi middleware
coinbase-cdp>=1.0.0                       # CDP SDK for wallet ops (optional)

# ─── Datadog Observability ───
# 4.8+ is critical -- it auto-instruments Pydantic AI tool calls + google-genai
ddtrace>=4.8.0
datadog>=0.50.1                           # for direct event/metric API

# ─── Resilience ───
tenacity==9.0.0

# ─── Scheduler ───
apscheduler==3.11.2

# ─── Logging ───
structlog==24.4.0

# ─── Synthetic Data ───
faker==30.7.0

# ─── CLI / Utilities ───
rich==13.9.4
click==8.1.7

# ─── Dev / Testing ───
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
respx==0.21.1            # httpx mocking
ruff==0.7.4              # linting + formatting
mypy==1.13.0             # type checking
