# ════════════════════════════════════════════════════════════════
# RegRadar — Python Dependencies
# Stack: Pydantic AI + ClickHouse Cloud + Nimble + OpenRouter + Lapdog
# ════════════════════════════════════════════════════════════════

# ─── Core Web ───
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
pydantic>=2.9.2
httpx>=0.27.2
aiohttp>=3.10.10

# ─── Pydantic AI (the agent framework) ───
# 1.85+ has GoogleVertexProvider, GoogleProvider, OpenRouterProvider.
# Newer versions removed the `input_type` kwarg the team's code used; the
# resolver in backend/integrations/vertex_ai.py works on 1.85+ and the
# crawler uses model_dump_json() to feed the agent.
pydantic-ai>=1.85.0,<2.0.0

# ─── LLM / AI ───
# google-genai 1.x keeps the .aio attribute that pydantic-ai's GoogleModel
# expects on the Vertex provider. 2.x removed it -- if you're stuck on 2.x,
# set OPENROUTER_API_KEY so the resolver picks the OpenRouter path.
google-genai>=1.40.0,<2.0.0
openai>=1.54.4                            # OpenRouter speaks the OpenAI API

# ─── Data ───
clickhouse-connect[arrow]>=0.8.0
pyarrow>=18.0.0

# ─── Web Scraping ───
# `nimble_python` is the Nimble web SDK on PyPI. `nimble-sdk` (no underscore)
# is HPE Nimble Storage and is unrelated.
nimble_python>=0.18.0
firecrawl-py>=1.6.7                       # silent fallback

# ─── Datadog Observability ───
ddtrace>=4.8.0                            # auto-instruments Pydantic AI + google-genai
datadog>=0.50.1                           # statsd gauges for the dashboard

# ─── Resilience ───
tenacity>=9.0.0

# ─── Scheduler ───
apscheduler>=3.11.0,<4.0.0

# ─── Logging ───
structlog>=24.4.0

# ─── Synthetic Data ───
faker>=30.0.0

# ─── CLI / Utilities ───
rich>=13.9.0
click>=8.1.0

# ─── Dev / Testing (optional) ───
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.7.0
mypy>=1.13.0
